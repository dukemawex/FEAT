# assets/

Place your static background image here before running the pipeline:

```
assets/background.jpg   ← JPEG image used as the video background (1280×720 recommended)
```

This file is **not** committed to source control because it is a binary asset.
Add it manually or via a GitHub Actions secret/cache step before running the workflow.
